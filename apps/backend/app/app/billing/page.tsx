// Real billing UI isn't built yet -- billing/layout.tsx renders ComingSoon
// instead of this whenever the "billing" feature gate is disabled (its
// current hardcoded state), which is unconditionally today.
export default function BillingPage() {
  return null;
}
