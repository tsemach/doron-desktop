// Moved to @workspace/backend-orm (shared with apps/office) -- re-exported
// here so every existing `from "../../database/schema"` import keeps working
// unchanged.
export * from "@workspace/backend-orm";
