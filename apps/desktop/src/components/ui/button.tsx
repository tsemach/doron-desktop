// Moved to @workspace/ui per docs/backend-saas/phase-2-shared-ui/design.md
// (adopting PR-5, ASC-105) -- re-exported here so every existing
// `from "@/components/ui/button"` import keeps working unchanged.
export { Button, buttonVariants } from "@workspace/ui";
